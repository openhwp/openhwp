[package]
name = "primitive"
version = "0.1.0"
edition = "2021"
description = "Primitive types for Korean document formats (HWP, HWPX)"
license = "MIT"

[dependencies]
serde = { version = "1.0", optional = true, features = ["derive"] }

[features]
default = []
serde = ["dep:serde"]
